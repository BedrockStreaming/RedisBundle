m6_redis:
    servers:
        server1:
            host:   'lolcathost'
            port: 6379
            timeout:   2               # timeout in second
    clients:
        default:
            servers:   ["server1"]     # list of servers to use
            prefix : raoul\          # namespace to use
