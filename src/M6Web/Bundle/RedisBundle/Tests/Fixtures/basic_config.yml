m6_redis:
    servers:
        server1:
            host:   'localhost'
            port: 6379
            timeout:   2               # timeout in second
    clients:
        default:
            servers:   ["server1"]     # list of servers to use
            namespace: raoul\          # namespace to use
