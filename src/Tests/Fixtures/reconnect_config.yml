m6web_redis:
    servers:
        server1:
            host:   'lolcathost'
            port: 6379
            timeout: 2
            reconnect: 2
    clients:
        default:
            servers:   ["server1"]     # list of servers to use
            prefix: raoul\          # namespace to use